{
    "name": "yma-config-inheriter",
    "version": "1.0.3",
    "description": "简易获取配置文件，支持 Presets & Plugins",
    "main": "./dist/index.js",
    "scripts": {
        "init": "rm -rf package-lock.json && npm install",
        "start": "npm run build -- -w",
        "build": "npm run clean && tsc -p src",
        "clean": "rm -fr dist"
    },
    "files": [
        "dist",
        "src"
    ],
    "devDependencies": {
        "@types/node": "17.0.22",
        "typescript": "4.2.3"
    }
}
